{
  "name": "hydrocred-contracts",
  "version": "1.0.0",
  "description": "HydroCred smart contracts",
  "license": "MIT",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network polygonAmoy",
    "export-abi": "ts-node scripts/export-abi.ts && echo 'ABI exported'"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.26.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
