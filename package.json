{
  "name": "hydrocred",
  "version": "1.0.0",
  "private": true,
  "description": "HydroCred - Blockchain-based Green Hydrogen Credit System",
  "workspaces": [
    "contracts",
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm -w blockchain i && npm -w backend i && npm -w frontend i",
    "dev": "concurrently -k \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "demo": "npm -w frontend run dev",
    "demo:full": "concurrently -k \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "chain:compile": "npm -w contracts run compile",
    "chain:deploy": "npm -w contracts run deploy:local",
    "chain:export-abi": "npm -w contracts run export-abi",
    "demo:wallets": "ts-node scripts/generate_demo_wallets.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
