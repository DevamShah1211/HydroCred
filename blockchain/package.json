{
  "name": "hydrocred-blockchain",
  "version": "1.0.0",
  "description": "HydroCred Smart Contracts",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "assign-admin": "hardhat run scripts/assign-state-admin.js --network sepolia",
    "assign-admin:local": "hardhat run scripts/assign-state-admin.js --network localhost",
    "postcompile": "node scripts/copy-abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/node": "^20.10.0",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.3.0",
    "ethers": "^6.8.0",
    "nodemon": "^3.1.10"
  }
}
